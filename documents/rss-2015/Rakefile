desc "build the pdf"
task :default => [] do
  sh "pdflatex -shell-escape model.tex"
  sh "pdfcrop model.pdf"
  sh "pdflatex rss.tex"
  sh "bibtex rss"
  sh "pdflatex rss.tex"
end

task :clean do
  sh "rm -rf *.aux *.bbl *.blg *.log"
end
